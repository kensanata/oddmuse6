<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Changes</title>
    <link rel="stylesheet" href="/css/default.css">
  </head>
  <body>

    <div id="header">
      <ul>
				{{#pages}}
				<li><a href="/view/{{id}}">{{id}}</a></li>
				{{/pages}}
      </ul>
    </div>

    <div id="content">
			<div id="changes">
				<h1>{{id}}</h1>
				{{#filter}}
				<strong>Filtered by:</strong>
				<ul id="filter">
					{{#name}}<li>{{name}}</li>{{/name}}
					{{#author}}<li>written by {{author}}</li>{{/author}}
					{{#minor}}<li>including minor changes</li>{{/minor}}
					{{#n}}<li>the last {{n}} entries</li>{{/n}}
					{{#all}}<li>list all changes (not just the last one)</li>{{/all}}
				</ul>
				{{/filter}}

				<ul id="items">
				{{#changes}}
				<li>
					{{date}} {{time}}
					{{^filter.name}}
					(<a href="/history/{{name}}">history</a>)
					{{/filter.name}}
					<a href="/view/{{name}}/{{to}}">{{name}}</a> …
					{{author}}
					{{^author}}<span class="ip-code">{{#code}}<span class="c{{c}}">{{c}}</span>{{/code}}</span>{{/author}}
					{{#summary}}
					– {{summary}}
					{{/summary}}
					{{#minor}}(minor){{/minor}}
				</li>
				{{/changes}}
				</ul>
				{{^changes}}
				<strong>There are no changes.</strong>
				{{/changes}}
			</div>
    </div>

    <div id="footer">
      <hr/>
      <p>
				<a href="/">Home</a>
      </p>
    </div>

  </body>
</html>
